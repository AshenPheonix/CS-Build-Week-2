import styled from 'styled-components';
import * as v from './global';

export const PickupModal=styled(v.GenModal)`

`
export const CloseButton=styled(v.GenCloseButton)`

`

export const PickupModalBody=styled(v.DefaultModalBody)`

`

export const ItemButton=styled(v.DefaultSelectedButton)`

`